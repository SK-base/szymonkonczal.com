# Plunk (useplunk.com) – newsletter / mailing list. Server-side only; never expose to client.
# Get your key from https://app.useplunk.com → Project → API Keys.
# On Vercel: Project → Settings → Environment Variables → add PLUNK_API_KEY.
PLUNK_API_KEY=
